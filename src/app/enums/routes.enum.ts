export enum Routes {
  LOGIN = '/login',
  START = '/start',
  TASK = '/task',
  DOCUMENT = '/document',
  SAVING = '/request-saving',
  CDT = '/request-cdt/:task',
  CREATE_CDT = '/create-cdt',
  FINISH = '/finish'
}
